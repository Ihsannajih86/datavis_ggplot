---
title: "visualisasi data dengan ggplot2"
author: "Muhammad Ihsan Najih Amani-Institut Teknologi Statistika dan Bisnis Muhammadiyah"
date: "`r Sys.Date()`"
output: 
  html_document:
    css: style.css
    higlight: zenburn
    toc: true
    toc_float:
      collapsed: true
    df_print: paged
bibliography: references.bib
---

```{=html}
<style>
body{
text-align: justify}
</style>
```
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# R Markdown

laman ini saya buat untuk menampilkan hasil penggolahan data yang telah diolah untuk memudahkan pengunjung web ini memahami data dengan mudah.

berikut hasil tampilan dari data yang telah saya olah dengan microsoft excel :


```{r}
#muat library ggplot2
library(ggplot2)

#buat data frame contoh
data <- data.frame(x = rnorm(100), y = rnorm(100))

#buat plot titik
ggplot(data, aes (x = x, y = y )) + 

#buat plot titik dengan label dan tema kustom
geom_point() + 
  labs(title = "plot titik", x = "sumbu x", y = "sumbu y") +
  theme_dark()

```
```{r}
# Buat data frame
data <- data.frame(
  x = rnorm(100),
  y = rnorm(100),
  grup = sample(letters[1:4], 100, replace = TRUE)  # Kelompok: "a", "b", "c", "d"
)

# Plot dengan tema warna Yae Miko
library(ggplot2)

ggplot(data, aes(x = x, y = y, color = grup)) +
  geom_point(size = 3) +
  facet_wrap(~ grup) +
  scale_color_manual(values = c(
    "a" = "#FFB6C1",  # Pink lembut (inspirasi warna rambut Yae Miko)
    "b" = "#9370DB",  # Ungu medium (tema Electro Vision)
    "c" = "#FFD700",  # Emas (aksen detail Yae Miko)
    "d" = "#BA55D3"   # Ungu gelap (kekuatan elegan)
  )) +
  theme_minimal() +
  labs(
    title = "Faceted Plot dengan Warna Yae Miko",
    x = "X",
    y = "Y"
  )

```
```{r}
library(readr)
lizards <- read.csv("lizards.csv")
View(lizards)
# Load library ggplot2
library(ggplot2)

# Ngeload data
lizards <- read.csv("lizards.csv")

# Tampilin data
View(lizards)

# Nggunakake ggplot kanggo plot data
ggplot(lizards, aes(x = total_length, y = weight)) +
  geom_point(color = "pink", size = 3, alpha = 0.6) +  # Titik nganggo warna pink
  labs(title = "Plot Titik - Lizard Data", x = "Total Length", y = "Weight") +
  theme_minimal() +  # Tema minimalis
  theme(
    plot.title = element_text(hjust = 0.5, size = 16, face = "bold", color = "darkred"),  # Judul nganggo warna merah tua
    axis.title = element_text(size = 12, color = "darkviolet"),  # Judul sumbu nganggo ungu
    axis.text = element_text(color = "purple")  # Teks axis nganggo ungu
  )

```


```{r}

```


```{r}
print('pengolahan data berdasarkan ')
```
```{r}
library(readr)
iris <- read_csv("iris.csv")
View(iris)
```

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

# Referensi
